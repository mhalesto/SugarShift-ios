# Level/objective/progress implementation brief

Own Levels.swift, new LevelDefinition.swift/ObjectiveTracker.swift/CampaignProgress.swift, and new LevelDefinitionTests.swift only. Main owns all GameScene files, HUD, theme catalog, asset pipeline. Another worker owns Engine.swift, model files, factory/simulation/TurnStrategy. Coordinate with main; no other agents, no git mutations, no localization edits. Preserve all 200 campaign IDs plus daily/Rush/Tower. Existing saves/economy/cloud keys must never reset.

Add a Codable LevelDefinition with id, boardWidth/Height, activeCells, pieceTypes (PieceColor seven cases being provided by engine worker), moves, objectives, fixed blockers, portals/conveyors, star thresholds, difficulty, rewards, allowed boosters, spawnWeights and optional tutorial. Validation rejects invalid masks, disconnected/invalid portal pairs, no playable moves geometry, zero colors/weights, impossible objective target configuration. Bundle JSON level overrides under SugarShift/LevelData if useful; expand existing data structures instead of whole rewrite. Main will adapt factory to definition if worker unavailable; report needed hook.

Extend LevelConfig with optional definition/default-compatible computed fields (`objectives`, `pieceTypes`, `spawnWeights`, `tutorial`) without breaking existing initializers. First baseline level is 6 rows by7 columns, three existing colors orange/grape/blueberry, 28 moves and THREE collection objectives count10 each (specified values in level data, not HUD). Gentle first15 progression no blocker until16; striped tutorial6, wrapped11, colorBomb12. Keep existing200 generator for later levels but gradual world-aligned mechanic data can override. World grouping main owns: candy1–15, ice16–30, honey31–45, volcano46–60, coral61–75, cloud76–90, golden91–110, firefly111–130, sahara131–150, galaxy151–175, sakura176–200. Do not add independent theme selectors.

ObjectiveTracker value type consumes pure events from actual clears/damage/special creations/collected drops/score, supports multiple simultaneous objectives: collectPieces, clearJelly, breakIce, breakBlockers, collectIngredients, reachScore, collectKeys, freePieces, clearChocolate, destroySpecificBlocker, comboObjective. Targets from definition. Provide explicit readable API to main with progress per objective and complete/progress, snapshot-safe for Undo. Legacy LevelGoal adapts to objectives without losing chest/mixed drop goals or boss shield requirement. Do not silently substitute unrelated goal values.

Stars need score thresholds: 0–3 from per-level scores (winning grants progression regardless of zero/1star; 3star never required). Existing star-based analytics/milestone logic compatible; LevelScoring currently has alternative performance ratings for objectives, inspect and adapt carefully. Preserve existing intentional tests or update only requirements-changed expectations. Add per-level bestScore, attempts, completion date using new additive local record key; never overwrite existing coins/lives/stars. Legacy existing stars can seed completed status/read fallback. Main hooks actual level start/completion. Keep cloud snapshot compatibility and identify how new supplemental records sync, if not included explicitly report limitation.

Write meaningful tests for objective completion, decoding validation, first board geometry/targets, score stars independent from objective victory, replay unlock monotonicity and legacy-save preservation. Coordinate Xcode builds with main. Return APIs early, test evidence and exact remaining scene/factory hooks.
